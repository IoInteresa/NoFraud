const verificationDao = require("./VerificationDao");
const productsDao = require("./ProductsDao");
const logsDao = require("./LogsDao");

module.exports = {
  verificationDao,
  productsDao,
  logsDao,
};
